//| mill-version: 1.0.5
//| mvnDeps:
//|   - "digital.junkie::mill-explicit-dependencies:1.0.5-SNAPSHOT"

package build
import mill.*, scalalib.*
import digital.junkie.milledependencies.ExplicitDependencies

object app extends ScalaModule with ExplicitDependencies {
  def scalaVersion = "3.8.2"
  override def mvnDeps = Seq(
    mvn"org.typelevel::cats-effect:3.6.3",
    mvn"co.fs2::fs2-core:3.12.2"
  )

  override def moduleDeps = Seq(
    lib
  )
}

object lib extends ScalaModule with ExplicitDependencies {
  def scalaVersion = "3.8.2"
  override def mvnDeps = Seq(
    mvn"org.typelevel::cats-effect:3.6.3",
    mvn"co.fs2::fs2-core:3.12.2"
  )
}
